# Foundry
contracts/out/
contracts/cache/
# Broadcast artifacts are local run records containing sender addresses and raw
# calldata. Useful locally, noise in the repo. Keep real deployment addresses in
# packages/config/chains.json instead, which is reviewed and intentional.
contracts/broadcast/
contracts/.env

# Node
node_modules/
web/.next/
web/out/
web/tsconfig.tsbuildinfo
.env
.env.local
.DS_Store

# Local dev chain. `npm run devchain` writes throwaway anvil addresses into
# web/.env.local, covered by the .env.local rule above. Real deployments go in
# packages/config/chains.json, which is reviewed and intentional.
web/.anvil.log
